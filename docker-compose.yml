services:
  ai-assistant:
    build: .

    container_name: ai-assistant

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./chat.db:/app/chat.db

    restart: unless-stopped